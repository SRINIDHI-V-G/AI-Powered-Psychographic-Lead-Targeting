fastapi==0.110.0
uvicorn[standard]==0.27.0
sqlalchemy[asyncio]==2.0.28
asyncpg==0.29.0
pydantic==2.6.1
pydantic-settings==2.2.0
email-validator==2.1.0
python-dotenv==1.0.0
httpx==0.27.0

# ── Phase B1: Discovery ───────────────────────────────────────────────────────
# Reddit API wrapper (read-only, application-only OAuth — no user login needed)
praw==7.7.1
# Alembic for versioned database migrations
alembic==1.13.1
# Synchronous PostgreSQL driver used by Alembic (app still uses asyncpg)
psycopg2-binary==2.9.9

# ── Phase C: NLP Pipeline ────────────────────────────────────────────────────
# Sentence-transformer embeddings (all-MiniLM-L6-v2, 384-dim)
sentence-transformers==2.7.0
# Empath emotional/topic category lexicon
empath==0.89
# Note: spaCy (3.7/3.8) has a pydantic-v1-compat / Python 3.12 conflict.
# Basic text metrics (entities, vocab richness) are implemented in pure Python.
# spaCy can be re-evaluated for a future hardening phase.
